namespace Snaipe.SampleApp;

public static class Program
{
    [STAThread]
    public static void Main(string[] args)
    {
        // TODO: initialize Uno Skia Gtk host, create a window with sample UI,
        //       then call SnaipeAgent.Attach(window) to start the inspector agent.
        Console.WriteLine("Snaipe SampleApp — not yet wired up.");
    }
}

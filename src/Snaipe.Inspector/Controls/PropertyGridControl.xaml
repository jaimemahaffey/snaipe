<UserControl
    x:Class="Snaipe.Inspector.Controls.PropertyGridControl"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Snaipe.Inspector.Controls"
    xmlns:vm="using:Snaipe.Inspector.ViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">

    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisConverter"/>
        <local:BoolToOpacityConverter x:Key="BoolToOpacityConverter"/>

        <!--
            Editor templates use {Binding} (not {x:Bind}) so that Uno's DataGrid can resolve the
            DataContext at runtime regardless of how the CVS-grouped ICollectionView delivers items.
            {x:Bind} with x:DataType inside a DataGrid CellTemplate can silently fail in Uno/Skia
            when the DataContext is delivered as 'object' through the grouping layer.
        -->
        <local:PropertyEditorTemplateSelector x:Key="EditorSelector">
            <local:PropertyEditorTemplateSelector.ReadOnlyTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Entry.Value}"
                               FontSize="12"
                               TextTrimming="CharacterEllipsis"
                               VerticalAlignment="Center"/>
                </DataTemplate>
            </local:PropertyEditorTemplateSelector.ReadOnlyTemplate>

            <local:PropertyEditorTemplateSelector.TextTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding EditValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="12"
                             BorderBrush="{Binding ErrorBorderBrush, Mode=OneWay}"
                             BorderThickness="1"
                             ToolTipService.ToolTip="{Binding ErrorMessage}"/>
                </DataTemplate>
            </local:PropertyEditorTemplateSelector.TextTemplate>

            <local:PropertyEditorTemplateSelector.BooleanTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsCheckedValue, Mode=TwoWay}"
                              VerticalAlignment="Center"/>
                </DataTemplate>
            </local:PropertyEditorTemplateSelector.BooleanTemplate>

            <local:PropertyEditorTemplateSelector.NumberTemplate>
                <DataTemplate>
                    <NumberBox Value="{Binding NumberValue, Mode=TwoWay}"
                               SpinButtonPlacementMode="Compact"
                               FontSize="12"/>
                </DataTemplate>
            </local:PropertyEditorTemplateSelector.NumberTemplate>

            <local:PropertyEditorTemplateSelector.EnumTemplate>
                <DataTemplate>
                    <ComboBox ItemsSource="{Binding Entry.EnumValues}"
                              SelectedItem="{Binding EditValue, Mode=TwoWay}"
                              FontSize="12"/>
                </DataTemplate>
            </local:PropertyEditorTemplateSelector.EnumTemplate>
        </local:PropertyEditorTemplateSelector>

        <Style x:Key="BreadcrumbButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="2,2"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>

        <Style x:Key="ChevronButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="2,0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Breadcrumb row — visible when drilled into a nested object -->
        <ScrollViewer Grid.Row="0"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled"
                      Margin="4,2,4,0">
            <ItemsControl ItemsSource="{x:Bind Host.Breadcrumb, Mode=OneWay}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:BreadcrumbSegment">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="{x:Bind Label}"
                                    Command="{x:Bind NavigateCommand}"
                                    Style="{StaticResource BreadcrumbButtonStyle}"/>
                            <TextBlock Text=" › "
                                       FontSize="11"
                                       Opacity="0.5"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Search bar -->
        <TextBox x:Name="SearchBox"
                 Grid.Row="1"
                 PlaceholderText="Search properties..."
                 Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="12"
                 Margin="4,4,4,2"/>

        <!-- Property rows — ItemsSource is set imperatively in code-behind via GroupedItemsCvs -->
        <controls:DataGrid x:Name="PropertyDataGrid"
                           Grid.Row="2"
                           AutoGenerateColumns="False"
                           CanUserSortColumns="True"
                           CanUserReorderColumns="False"
                           CanUserResizeColumns="True"
                           HeadersVisibility="Column"
                           GridLinesVisibility="Horizontal"
                           SelectionMode="Single"
                           RowDetailsVisibilityMode="Collapsed"
                           RowHeight="28">
            <controls:DataGrid.RowGroupHeaderStyles>
                <Style TargetType="controls:DataGridRowGroupHeader">
                    <Setter Property="Background" Value="#1E293B"/>
                    <Setter Property="Foreground" Value="#9CA3AF"/>
                    <Setter Property="FontSize" Value="11"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <!-- Hide the hardcoded "Group" prefix; PropertyValue carries the category name -->
                    <Setter Property="PropertyNameVisibility" Value="Collapsed"/>
                </Style>
            </controls:DataGrid.RowGroupHeaderStyles>
            <controls:DataGrid.Columns>
                <!-- Name Column (Includes drill-down controls and lock icon) -->
                <controls:DataGridTemplateColumn Header="NAME" Width="2*">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid Padding="4,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="🔒"
                                           FontSize="10"
                                           Foreground="#9CA3AF"
                                           VerticalAlignment="Center"
                                           Margin="0,0,4,0"
                                           Visibility="{Binding Entry.IsReadOnly, Converter={StaticResource BoolToVisConverter}}"/>

                                <TextBlock Grid.Column="1"
                                           Text="{Binding Entry.Name}"
                                           FontSize="12"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"
                                           Opacity="{Binding Entry.IsReadOnly, Converter={StaticResource BoolToOpacityConverter}}"/>

                                <StackPanel Grid.Column="2"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <Button Content="?"
                                            Visibility="{Binding ShowValueChainVisibility}"
                                            Command="{Binding ShowValueChainCommand}"
                                            Style="{StaticResource ChevronButtonStyle}"/>
                                    <Button Content="↗"
                                            Visibility="{Binding JumpToTemplateVisibility}"
                                            Command="{Binding JumpToTemplateCommand}"
                                            Style="{StaticResource ChevronButtonStyle}"/>
                                    <Button Content="›"
                                            Visibility="{Binding DrillVisibility}"
                                            Command="{Binding DrillCommand}"
                                            Style="{StaticResource ChevronButtonStyle}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <!-- Value Column -->
                <controls:DataGridTemplateColumn Header="VALUE" Width="2*">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ContentPresenter Content="{Binding}"
                                              ContentTemplateSelector="{StaticResource EditorSelector}"
                                              Margin="4,2"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <!-- Type Column -->
                <controls:DataGridTextColumn Header="TYPE" Width="1*" Binding="{Binding Entry.ValueType}" FontSize="10" Foreground="#9CA3AF" />
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <!-- Value chain panel — visible when a ? button is active -->
        <Grid Grid.Row="3"
              Visibility="{x:Bind ViewModel.ValueChainPanelVisibility, Mode=OneWay}"
              BorderBrush="#2563EB"
              BorderThickness="0,2,0,0"
              Background="#0F172A"
              Padding="12,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Panel header: property name + close button -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <TextBlock Text="{x:Bind ViewModel.ValueChainPropertyName, Mode=OneWay}"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="#60A5FA"
                           VerticalAlignment="Center"/>
                <Button Content="✕"
                        HorizontalAlignment="Right"
                        Command="{x:Bind ViewModel.ClearValueChainCommand}"
                        Style="{StaticResource ChevronButtonStyle}"/>
            </Grid>

            <!-- Chain entries list -->
            <ItemsControl Grid.Row="1"
                          ItemsSource="{x:Bind ViewModel.ActiveValueChain, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:ValueChainEntryViewModel">
                        <Grid Margin="0,2">

                            <!-- Winner row: green left border, blue tint background, "wins" badge -->
                            <Border Background="#1E3A5F"
                                    CornerRadius="4"
                                    BorderBrush="#22C55E"
                                    BorderThickness="3,0,0,0"
                                    Padding="8,5"
                                    Visibility="{x:Bind WinnerBadgeVisibility}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal"
                                                VerticalAlignment="Center"
                                                Spacing="4">
                                        <TextBlock Text="{x:Bind Source}"
                                                   FontSize="10"
                                                   FontWeight="SemiBold"
                                                   Foreground="#22C55E"/>
                                        <Border Background="#14532D"
                                                CornerRadius="3"
                                                Padding="3,1">
                                            <TextBlock Text="wins"
                                                       FontSize="9"
                                                       Foreground="#86EFAC"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1"
                                               Text="{x:Bind Value}"
                                               FontSize="11"
                                               Foreground="#4ADE80"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <!-- Overridden row: grey left border, dimmed, strikethrough value -->
                            <Border Background="#1F2937"
                                    CornerRadius="4"
                                    BorderBrush="#374151"
                                    BorderThickness="3,0,0,0"
                                    Padding="8,5"
                                    Opacity="0.6"
                                    Visibility="{x:Bind OverriddenVisibility}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{x:Bind Source}"
                                               FontSize="10"
                                               Foreground="#9CA3AF"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1"
                                               Text="{x:Bind Value}"
                                               FontSize="11"
                                               Foreground="#9CA3AF"
                                               TextDecorations="Strikethrough"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

    </Grid>
</UserControl>
